import React from 'react';
import Button from '@mui/material/Button';

import './button_exit.css';
function Button_Exit() {
  return (
    <Button className="exit" variant="contained">
      Выход
    </Button>
  );
}

export default Button_Exit;
